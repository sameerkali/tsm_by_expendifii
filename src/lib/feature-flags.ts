/**
 * Code-only toggles — no UI control. Flip the value and redeploy.
 */

/**
 * Homepage hero background. `true` (default) renders the animated beam
 * effect; `false` falls back to the static grid pattern.
 */
export const SHOW_HERO_BEAM = true;
